export const API_URL = 'https://jsonplaceholder.typicode.com/'

export const QUERIES = {
    limit: `_limit`
}

export const PATH = {
    posts: 'posts',
    comments: 'comments',
}

export const LIMITS = {
    posts: 10,
    comments: 2,
}